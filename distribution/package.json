{
  "name": "lerro-distribution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "node --check src/worker.mjs && node --check src/publication-batch.mjs && node --check bin/render-publication-sql.mjs",
    "render-publication-sql": "node bin/render-publication-sql.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
